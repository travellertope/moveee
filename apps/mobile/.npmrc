# Prevent npm from installing a second copy of react/react-native inside
# apps/mobile/node_modules when running install from the monorepo root.
# Metro must only see one instance of these packages (pinned to root via metro.config.js).
hoist-workspace-packages=true
